# エージェント作業宣言 (co-working note)

このリポジトリでは **複数の Claude Code セッションが同時に作業している**。
衝突を避けるため、各セッションはここに担当ファイルを宣言する。

作業を始める前にこのファイルを読み、**他セッションが「所有」しているファイルは触らない**こと。
自分の担当が終わったら、該当セクションを「完了」に書き換える。

---

## セッション B — ビジュアル層の強化 (2026-08-02 10:5x 開始 / 進行中)

**担当範囲: 描画層のみ。ロジック・エージェント・学習には一切触らない。**

### 所有ファイル (他セッションは編集しないでほしい)

| ファイル | 状態 | 内容 |
|---|---|---|
| `tetris_vla/pinballviz.py` | **編集中** | ラダー図の新規描画、通電状態表示、制御層の全体図、盤面 SVG の改善 |

### 読むだけ / 変更しないファイル

- `tetris_vla/pinball.py` — `Rung` / `build_ladder()` を **参照のみ**。データ構造は変えない
- `tetris_vla/pinball_agents.py` — **触らない**（セッション A が編集中と認識している）
- `scripts/*.py`, `tests/*.py` — 触らない

### やっていること

`build_ladder()` が返す 6 本のラングは、これまで HTML の表に条件式のテキストとして
出ているだけで、**ラダー図そのもの（母線・a 接点・b 接点・並列枝・自己保持・コイル）が
描かれていなかった**。ここを SVG で描く。

1. `ladder_diagram()` — 静的なラダー図（母線 / `─┤├─` / `─┤/├─` / `─( )─`）
2. 通電状態のオーバーレイ — `ladder_trace` のビットで ON の接点・コイルを発光させる
3. 制御層の全体図 — ラダー / シーケンサ / VLA / VLM のクロック差を 1 枚に
4. 盤面 SVG (`pinball_svg`) の改善

既存の `ladder_timing_chart()` と HTML の表は **消さずに残す**
（表は `comment` 列を持っており、図では表現できない情報があるため）。

### git について

**このセッションは commit も push もしない。** 変更はワーキングツリーに置いたままにする。
コミットのタイミングは人間が決める。

> セッション A へ: もし先に commit する場合、`git add -A` ではなく
> **パス指定で自分の担当ファイルだけを stage** してほしい。
> こちらの編集途中の `pinballviz.py` が巻き込まれて push されるのを避けたい。

---

## セッション A — ピンボール / VLA 本体 (認識している範囲)

こちらから観測できた情報のみ。正確なところは当該セッションが上書きしてほしい。

- `tetris_vla/pinball_agents.py` を編集中（VLM に渡す画像の縮尺 `px_per_unit`、メモリ対策）
- `results/pinball_vlm.*` を生成
- 過去に `origin/main` へ push 実績あり
